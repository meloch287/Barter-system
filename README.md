# Решение тестового задания для позиции Junior Backend Developer

Проект представляет собой бартерную платформу для обмена вещами между пользователями с веб-интерфейсом и REST API.

# Описание 📝

 "Barter Platform" пользователи могут размещать объявления о товарах, искать чужие предложения и отправлять запросы на обмен. Платформа включает:

- Хранение информации об объявлениях: заголовок, описание, категория, состояние, изображение, автор

- Возможность отправки предложений обмена между пользователями

- Веб-интерфейс для CRUD-операций (создание, редактирование, удаление, просмотр)

- REST API для управления объявлениями и предложениями

- Поиск и фильтрация по ключевым словам, категории и состоянию товара


# Установка ⚙️

1. Клонируйте репозиторий:

```
git clone https://github.com/meloch287/Barter-system.git
```

2. Перейдите в директорию проекта:
```
cd Barter-system
```

3. Установите зависимости:
```
pip install -r requirements.txt
```

4. Настройте базу данных (по умолчанию используется SQLite, можно заменить на PostgreSQL в settings.py)

5. Выполните миграции:

```
python manage.py migrate
```
6. Запустите сервер:
```
python manage.py runserver
```
сервер будет доступен по адресу: http://127.0.0.1:8000/

      
# Использование ☀️
### Веб-интерфейс

- Список объявлений: отфильтрованный список всех объявлений по категории, ключевым словам или состоянию

- Создание объявления: форма с полями (заголовок, описание, изображение, категория, состояние)

- Редактирование и удаление: доступны только автору объявления

- Просмотр объявлений: детальная информация с кнопкой «Предложить обмен»

- Обмен: форма отправки предложения обмена с комментарием. Статус — «ожидает» по умолчанию

- Управление предложениями: просмотр, принятие или отклонение предложений


---

# REST API
API доступно по префиксу /api/

### 1. Добавление объявления
- Метод: POST
- URL: ```/api/ads/```
- Описание: Создает новое объявление. Требуется аутентификация (токен или сессия).  
- Параметры (JSON):
   - `title` (string, обязательный) — заголовок объявления.
   - `description` (string, обязательный) — описание объявления.
   - `image_url` (string, опциональный) — URL изображения.
   - `category` (string, обязательный) — категория объявления.
   - `condition` (string, обязательный) — состояние вещи (`new`, `used`, `refurbished`).

Пример запроса:
```
{
  "title": "Книга 'Война и мир'",
  "description": "Хорошее состояние, мягкая обложка",
  "image_url": "http://example.com/book.jpg",
  "category": "Книги",
  "condition": "used"
}
```
Пример ответа:  
```
{
  "id": 1,
  "title": "Книга 'Война и мир'",
  "description": "Хорошее состояние, мягкая обложка",
  "image_url": "http://example.com/book.jpg",
  "category": "Книги",
  "condition": "used",
  "user": 1,
  "created_at": "2025-04-17T10:00:00Z"
}
```


### 2. Удаление объявления
- Метод: DELETE
- URL: `/api/ads/<id>/` (где `<id>` — идентификатор объявления).
- Описание: Удаляет объявление. Доступно только владельцу объявления. Требуется аутентификация.
  
Пример запроса:  

``` 
DELETE http://localhost:8000/api/ads/1/
```

Пример ответа:

(Статус 204 No Content, без тела ответа)

### 3. Поиск и отображение объявлений
- Метод: GET
- URL: ```/api/ads/?q=<query>&category=<category>&condition=<condition>```
- Описание: Возвращает список объявлений с возможностью фильтрации по поисковому запросу, категории и состоянию.  
- Параметры (опционально):
   - ```q``` (string) — поисковый запрос (поиск по заголовку или описанию).
   - ```category``` (string) — категория объявления.
   - ```condition``` (string) — состояние вещи (`new`, `used`, `refurbished`).

Пример запроса:  
```
GET http://localhost:8000/api/ads/?q=книга&condition=used
```
Пример ответа:  

```
[
  {
    "id": 1,
    "title": "Книга 'Война и мир'",
    "description": "Хорошее состояние, мягкая обложка",
    "image_url": "http://example.com/book.jpg",
    "category": "Книги",
    "condition": "used",
    "user": 1,
    "created_at": "2025-04-17T10:00:00Z"
  },
  {
    "id": 2,
    "title": "Книга '1984'",
    "description": "Почти новая, твердая обложка",
    "image_url": null,
    "category": "Книги",
    "condition": "used",
    "user": 2,
    "created_at": "2025-04-16T12:00:00Z"
  }
]
```


### 4. Изменение объявления
- Метод: PATCH или PUT
- URL: ```/api/ads/<id>/```
- Описание: Обновляет данные объявления. Доступно только владельцу. Требуется аутентификация.  
- Параметры (JSON): Обновляемые поля, например:
   - `title` (string, опциональный).
   - `description` (string, опциональный).
   - `image_url` (string, опциональный).
   - `category` (string, опциональный).
   - `condition` (string, опциональный).

Пример запроса:  

```
{
  "description": "Отличное состояние, мягкая обложка",
  "condition": "refurbished"
}
```

Пример ответа:  
```
{
  "id": 1,
  "title": "Книга 'Война и мир'",
  "description": "Отличное состояние, мягкая обложка",
  "image_url": "http://example.com/book.jpg",
  "category": "Книги",
  "condition": "refurbished",
  "user": 1,
  "created_at": "2025-04-17T10:00:00Z"
}
```

### 5. Создание предложения обмена
- Метод: POST
- URL: `/api/proposals/`
- Описание: Создает предложение обмена между двумя объявлениями. Требуется аутентификация.  
- Параметры (JSON):
   - `ad_sender_id` (int, обязательный) — ID объявления отправителя (должно принадлежать текущему пользователю).
   - `ad_receiver_id` (int, обязательный) — ID объявления получателя.
   - `comment` (string, опциональный) — комментарий к предложению.

Пример запроса:  
```

{
  "ad_sender_id": 1,
  "ad_receiver_id": 2,
  "comment": "Предлагаю обмен на вашу книгу"
}
```

Пример ответа:  
```

{
  "id": 1,
  "ad_sender_id": 1,
  "ad_receiver_id": 2,
  "comment": "Предлагаю обмен на вашу книгу",
  "status": "pending",
  "created_at": "2025-04-17T10:30:00Z"
}

```

### 6. Изменение статуса предложения
- Метод: PATCH или PUT
- URL: `/api/proposals/<id>/`
- Описание: Обновляет статус предложения (`pending`, `accepted`, `rejected`). Доступно только владельцу объявления-получателя. Требуется аутентификация.  
- Параметры (JSON):
   - `status` (string, обязательный) — новый статус (`accepted` или `rejected`).

Пример запроса:  

```
{
  "status": "accepted"
}
```

Пример ответа:  
```
{
  "id": 1,
  "ad_sender_id": 1,
  "ad_receiver_id": 2,
  "comment": "Предлагаю обмен на вашу книгу",
  "status": "accepted",
  "created_at": "2025-04-17T10:30:00Z"
}
```


### 7. Получение списка предложений
- Метод: GET
- URL: `/api/proposals/?type=<type>`
- Описание: Возвращает список отправленных или полученных предложений для текущего пользователя. Требуется аутентификация.  
- Параметры (опционально):
   - `type` (string) — тип предложений (`sent` для отправленных, `received` для полученных).

Пример запроса:  
```
GET http://localhost:8000/api/proposals/?type=received
```

Пример ответа:  
```
[
  {
    "id": 1,
    "ad_sender_id": 1,
    "ad_receiver_id": 2,
    "comment": "Предлагаю обмен на вашу книгу",
    "status": "pending",
    "created_at": "2025-04-17T10:30:00Z"
  },
  {
    "id": 2,
    "ad_sender_id": 3,
    "ad_receiver_id": 2,
    "comment": "Обменяю на ноутбук",
    "status": "rejected",
    "created_at": "2025-04-16T15:00:00Z"
  }
]
```



# Архитектура проекта 📂

```
barter_platform/
│
├── barter_platform/                  
│   ├── __init__.py  
│   ├── settings.py                    # Настройки проекта (база данных, приложения, middleware)
│   ├── urls.py                        # Основной файл маршрутов проекта
│   └── wsgi.py                        #  для развертывания
│
├── ads/                               # Приложение для управления объявлениями и предложениями обмена
│   ├── __init__.py                   
│   ├── admin.py                       # Настройки админ-панели для моделей Ad и ExchangeProposal
│   ├── apps.py                        # Конфигурация приложения ads
│   ├── forms.py                       # Формы для создания/редактирования объявлений
│   ├── models.py                      # Модели данных (Ad, ExchangeProposal)
│   ├── urls.py                        # URL-маршруты приложения ads
│   ├── views.py                       # Представления для обработки запросов
│   ├── migrations/                   
│   │   ├── __init__.py               
│   │   └── 0001_initial.py           
│   ├── templates/                 
│   │   ├── ads/                   
│   │   │   ├── base.html            # Базовый шаблон
│   │   │   ├── ad_list.html         # Список объявлений
│   │   │   ├── ad_form.html         # Форма для создания/редактирования объявления
│   │   │   ├── ad_confirm_delete.html # Подтверждение удаления объявления
│   │   │   ├── proposal_form.html   # Форма для предложения обмена
│   │   │   └── proposal_list.html   # Список предложений обмена
│   └── tests.py                      # Тесты для приложения
│
├── templates/                       
│   ├── login.html                    # Страница входа (для django.contrib.auth)
│   └── logout.html                   # Страница выхода
│
├── static/                          # Стат файлы
│   └── css/                         # Подпапка для CSS
│       └── styles.css               # Стили
│
├── db.sqlite3                        # Файл бд
├── manage.py                         # Скрипт для управления проектом
├── requirements.txt                  # Зависимости
└── README.md                         # Документация проекта
```

# Стек технологий 🧰

- Python 3.8+

- Django 4+

- Django REST Framework (для API)

- SQLite или PostgreSQL

HTML/CSS (Django templates)
